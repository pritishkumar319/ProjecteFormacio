import { FilterIncidenciesPipe } from './filter-incidencies.pipe';

describe('FilterIncidenciesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterIncidenciesPipe();
    expect(pipe).toBeTruthy();
  });
});
